% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clv.R
\name{expanding_clv_window}
\alias{expanding_clv_window}
\title{Expanding-window devigged CLV with baseline comparison}
\usage{
expanding_clv_window(bets, odds, closing, scenario, seasons)
}
\arguments{
\item{bets}{Tibble from \code{ah_bets_from_preds()} with \code{match_id}. Can
be empty to produce a baseline-only result.}

\item{odds}{Tibble with \code{match_id}, \code{pahh}, \code{paha}, \code{ah_line}.}

\item{closing}{Tibble from \code{load_closing_ah_prices()} with \code{pcahh},
\code{pcaha}, \code{ah_line_close}.}

\item{scenario}{Character label for the bet tibble.}

\item{seasons}{Character vector of validation seasons to iterate
through (e.g. \code{c("2020-21","2021-22","2022-23")}). Each row of the
output is cumulative through \code{seasons[1:k]}.}
}
\value{
Tibble with one row per cumulative-through-season per
scenario, plus a matched-seasons baseline row per cumulative
window. Columns include \code{n_bets}, \code{devig_clv_pp},
\code{devig_clv_ci_lo}/\code{hi}, \code{devig_excess_pp} (scenario minus matched
baseline), \code{decimal_clv_pct} for reference.
}
\description{
Produces cumulative devigged CLV through each season in the validation
window, for a model bet tibble and an unselected same-line baseline.
Reveals whether a model's excess CLV is stable as the sample grows
(real signal) or shrinks (noise, regime artefact).
}
\details{
Devigging strips Pinnacle's vig from both opening and closing pair
prices via multiplicative normalisation: \code{fair = (1/p) / (1/pH + 1/pA)}.
The per-match devigged CLV is \code{fair_close_home - fair_open_home},
expressed in probability points.
}
\seealso{
Other decisions: 
\code{\link{ah_bets_from_preds}()},
\code{\link{apply_guardrails}()},
\code{\link{attach_clv}()},
\code{\link{clv}},
\code{\link{find_value_bets}()},
\code{\link{identify_value_bet}()},
\code{\link{kelly_fraction}()},
\code{\link{load_closing_ah_prices}()},
\code{\link{oagd_backtest}},
\code{\link{oagd_backtest_league}()},
\code{\link{oagd_backtest_summary}()},
\code{\link{oagd_edge}()},
\code{\link{oagd_grid}()},
\code{\link{oagd_pnl}()},
\code{\link{oagd_stake}()},
\code{\link{simulate_pnl}()},
\code{\link{summarise_ah_clv}()},
\code{\link{summarise_pnl}()}
}
\concept{decisions}
