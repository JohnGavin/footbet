% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_theme.R
\name{add_time_slider}
\alias{add_time_slider}
\title{Add range slider to time series plotly}
\usage{
add_time_slider(p)
}
\arguments{
\item{p}{A plotly object with a time-based x-axis.}
}
\value{
A plotly object with rangeslider enabled.
}
\description{
Enhance a time series plotly with an interactive range slider and range
selector buttons (1 year, 5 years, all data). Useful for exploring trends
across multiple seasons.
}
\examples{
\dontrun{
library(plotly)
library(dplyr)
df <- tibble(date = seq.Date(as.Date("2015-01-01"), by = "month", length.out = 100),
             value = cumsum(rnorm(100)))
plot_ly(df, x = ~date, y = ~value, type = "scatter", mode = "lines") |>
  add_time_slider()
}
}
\concept{plotting}
